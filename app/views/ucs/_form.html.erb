<%= form_for(@uc) do |f| %>
  <% if @uc.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@uc.errors.count, "error") %> prohibited this uc from being saved:</h2>

      <ul>
      <% @uc.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <br>
  <div class="actions">
    <%= f.submit class: 'btn btn-default'%>
  </div>
<% end %>
